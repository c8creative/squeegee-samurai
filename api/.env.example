# Server
PORT=3000

# Database (Supabase Postgres or any Postgres)
DATABASE_URL=postgresql://user:password@host:5432/dbname?sslmode=require

# CORS – allowed frontend origin(s), comma-separated for multiple
FRONTEND_ORIGIN=http://localhost:5173,https://squeegee-samurai.com

# Email – optional; if not set, notification is logged only
# SMTP (e.g. Gmail, SendGrid SMTP) - LEGACY, will be replaced by Resend
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=noreply@squeegee-samurai.com
# Owner receives the lead notification at:
NOTIFY_EMAIL=owner@example.com

# ============================================================
# PDF Generation & Email (Resend) - NEW
# ============================================================

# Supabase Storage (for PDF uploads)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here
SUPABASE_STORAGE_BUCKET=quotes

# Resend (modern email API, replaces SMTP)
RESEND_API_KEY=re_xxxxxxxxxxxxx
RESEND_FROM_EMAIL=quotes@squeegee-samurai.com

# PDF Settings
PDF_URL_EXPIRY_SECONDS=259200  # 72 hours (3 days)
